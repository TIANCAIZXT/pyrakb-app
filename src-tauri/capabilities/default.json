{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "PyraKB default capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-load-vault",
    "allow-sync-vault",
    "allow-reveal-vault"
  ]
}
